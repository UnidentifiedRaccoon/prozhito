# Task / Result Packet

## Task

**Название:** Шапки без меток и два варианта со светлой вуалью

**Владелец:** Стефания; read-only проверка контраста — design_references.

**Статус:** done — ожидает owner review

**Цель:** В существующей лаборатории убрать подписи типа экрана из всех шапок и добавить к пяти вариантам полупрозрачную врезку и кадр с заголовком поверх светлой вуали.

**Входные источники:**

- `AGENTS.md`, `harness/PROJECT_STATE.md`, `harness/WORKBOARD.md`, `harness/TASK_TEMPLATE.md`.
- `harness/tasks/2026-08-30-editorial-header-lab.md`, `src/apps/editorial-header-lab/README.md`.
- Текущие шапки и уточнение владельца от 2026-08-30; прежняя пара изображений и ранее исследованные референсы.

**Write set:**

- `src/apps/editorial-header-lab/HeaderComposition.tsx`.
- `src/apps/editorial-header-lab/HeaderLab.module.css`.
- `src/apps/editorial-header-lab/HeaderLabApp.tsx`.
- `src/apps/editorial-header-lab/model.ts`.
- `src/apps/editorial-header-lab/README.md`.
- Этот packet, `harness/PROJECT_STATE.md`, `harness/WORKBOARD.md`.

**Вне scope:**

- Рабочая editorial_v2, архив, канон и поведение цепочки; изображения не заменяются и не генерируются заново.
- Изменение прежних пяти композиций помимо удаления меток и адаптации общей панели к семи вариантам.
- Выбор победителя, перенос шапок, commit, push, внешняя публикация.

**Риски:**

- Полупрозрачность не должна ухудшать контраст текста; прозрачным делается фон, а не весь блок с заголовком.
- Длинный заголовок не должен выйти за область вуали или перекрыть навигацию.
- Семь переключателей должны оставаться доступными на 320 px без горизонтальной прокрутки.
- Запрос на полупрозрачность — разрешённое изменение только лабораторного визуального контракта; канон рабочей editorial_v2 не меняется.

**Дизайн-инвентарь:**

- Внутри всех шапок только h1, иллюстрация и навигация: без «История 01 / Решение 01».
- Внешние подписи сравнения — нейтральные «Первый экран / Второй экран», без повторного названия типа экрана.
- 06 `inset-light` / «Светлая врезка»: геометрия исходной врезки, фон #FCFCFB с 74% непрозрачности, изображение видно под ним.
- 07 `frame-veil` / «Кадр с вуалью»: центрированный заголовок в нижней части кадра на широкой светлой полосе с 64% непрозрачности. Полоса растёт вместе с заголовком, сохраняя область изображения сверху.
- Локальная итерация существующей системы: HTML/CSS без новой генерации, прежние frontend-app-builder / frontend-testing-debugging / browser / React принципы.

**План проверки:**

- TypeScript/build, маршруты семи вариантов, отсутствие старых меток в 14 шапках.
- Browser/IAB: обе новые пары на 320/390/1280 px, длинные заголовки; старые варианты после удаления подписей.
- Контраст поверх худшего возможного тёмного фона, реальная визуальная проверка изображения за вуалью.
- Переключатели, чтение, полная история/девять звеньев, отсутствие overflow и runtime errors.
- Whitespace, относительные ссылки, сохранность файлов вне scope.

## Result

**Итог:** Из всех семи композиций удалены видимые подписи «История 01 / Решение 01». Добавлены две пары: `#/lab/editorial-headers/inset-light` и `#/lab/editorial-headers/frame-veil`. Над окнами — «Первый экран / Второй экран»; убрано `Section 01` из описания лаборатории. Прежние пять композиций остаются доступны. Канон, изображения, production editorial_v2, архив и цепочка не менялись.

**Файлы:** Только восемь файлов объявленного write set: четыре файла реализации лаборатории, её README, этот packet, PROJECT_STATE и WORKBOARD.

**Проверки:**

- `npm run build` — PASS, включая TypeScript. Сохранилось предупреждение Vite о большом основном bundle (~616 kB); задача его не исправляет.
- Серверный render и маршруты: 56 сочетаний семи вариантов × двух экранов × preview/standalone × обычного/длинного названия. Один h1, нет старых меток, полное содержимое каждого типа экрана совпадает во всех вариантах, одна общая пара разных изображений — PASS.
- Контентный инвариант: 6 уровней, 22 Section, распределение 3/4/4/4/4/3, 48 внутренних действий, девять звеньев каждой Section — PASS.
- Browser/IAB: 44 измерения шапок. Обе новые пары с обычным и длинным названием на 320/390/1280 px; прежние пять пар с обычным названием на 320/1280 px. Все изображения загрузились; title помещается в свой блок, меток типа экрана нет, горизонтальная прокрутка отсутствует; ссылки в шапке высотой 44 px.
- Визуально просмотрены обе новые пары при сравнении, мобильные шапки, длинные заголовки на 320 px, широкие шапки и переход к реальному тексту. Саша и телефон остаются в кадре; вуаль пропускает изображение, но не делает буквы прозрачными. На 390 px собственная шапка без панели лаборатории заканчивается на 402 px у врезки и на 384 px у кадра с вуалью; ниже видно начало чтения.
- Панель семи вариантов: на 320/768/1280 px — 3/4/7 колонок, без горизонтальной прокрутки. Проверены клики 06/07, длинный заголовок, режим «Шире», открытие отдельного экрана, переход к решению и назад, выбор варианта с сохранением длинного названия. Второй экран содержит девять звеньев. Console warnings/errors не получены.
- Расчёт слоя #FCFCFB над чёрным с текстом #202522: около 8:1 при .74 и 6:1 при .64. Read-only review отдельным агентом не нашёл дефектов: flow заголовка увеличивает подложку, а не обрезается фиксированной высотой.
- 208 защищённых существующих файлов совпадают с SHA-256 baseline начала итерации, включая контент, production editorial_v2, архивные компоненты, App и router. Новых зависимостей нет.
- `git diff --check`, `git diff --cached --check`, whitespace новых файлов и относительные ссылки README/packet — PASS. Коммит, push и публикация не выполнялись.
- Это локальный дизайн-QA, не полный release-pass.

**Источники и даты:** Уточнение владельца и материалы лаборатории, 2026-08-30.

**Оставшиеся риски:** Реальные устройства и разные системные шрифты требуют отдельного review. У светлой врезки только верхние 82 px пересекают изображение; длинный заголовок ниже этой зоны читается на обычном фоне. У кадра с вуалью длинное название приглушает большую часть иллюстрации. Оба эффекта являются явными компромиссами вариантов.

**Следующий шаг:** Сравнить два новых варианта с исходными, без автоматического выбора.
