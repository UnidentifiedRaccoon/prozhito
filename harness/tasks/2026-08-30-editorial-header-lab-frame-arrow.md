# Task / Result Packet

## Task

**Название:** Одна стрелка в исходном «Кадре» 02

**Владелец:** Стефания; centered_frame_review — read-only review.

**Статус:** done

**Цель:** Добавить три варианта размещения одной ссылки-стрелки в исходном кадре02 вместо двух ссылок «Все истории» и «Прожито» в шапке. Сохранить все9существующих вариантов.

**Входные источники:**

- AGENTS.md, PROJECT_STATE.md, WORKBOARD.md, TASK_TEMPLATE.md, PRODUCT.md (навигация), DECISIONS.md (решения26/27), EDITORIAL_V2.md.
- Текущий frame/02 в браузере; [последняя итерация](2026-08-30-editorial-header-lab-two-line.md).
- Уточнение владельца от2026-08-30: предпочтителен исходный кадр, в шапке достаточно одной стрелки в общий маршрут.
- Ранее прочитанные Frontend App Builder / Frontend Testing / Browser и редакционные референсы лаборатории. Это точечная итерация существующей композиции: новые изображения не нужны. Impeccable недоступен и не используется.

**Write set:**

- src/apps/editorial-header-lab/model.ts.
- src/apps/editorial-header-lab/HeaderComposition.tsx.
- src/apps/editorial-header-lab/HeaderLab.module.css.
- src/apps/editorial-header-lab/HeaderLabApp.tsx.
- src/apps/editorial-header-lab/README.md.
- Этот packet; только абзацы лаборатории в harness/PROJECT_STATE.md и harness/WORKBOARD.md.

**Вне scope:**

- Удаление или переделка9прежних вариантов, включая frame/02.
- Канонические тексты, изображения, два экрана, девять звеньев и нижняя последовательная навигация.
- Production editorial_v2, архив, упражнение, экран каталога/новое поведение маршрута, прогресс и сохранение данных.
- Коммит, push, публикация, автоматический перенос в рабочую версию.

**Дизайн-контракт:**

- Новые G/H/I наследуют .frame, не .frameStudy: прямой заголовок, исходные размеры/боковые поля/линия/отступы и crop. Чтение сохраняет32px после шапки.
- G / frame-nav-top / «Над кадром»: одинокая стрелка без видимого контейнера в компактной56px полосе над изображением.
- H / frame-nav-overlay / «На картинке»: стрелка на непрозрачной светлой квадратной подложке,16px от верхнего края изображения. Отдельная верхняя полоса убрана.
- I / frame-nav-edge / «У кромки»: та же44px подложка пересекает нижний край изображения слева (на22px вниз); заголовок начинается ниже, его ширина и центр не меняются.
- Во всех новых шапках ровно одна ссылка:24px SVG-стрелка внутри44×44px цели, href `#/editorial-v2/`, target `_top`, доступное имя и tooltip «К маршруту историй». Это переход в существующий каталог первой главы; продолжение или прогресс не изобретаются.
- В обеих шапках Section одинаковое положение стрелки. Контрастная подложка, focus-visible, клавиатурный доступ; без теней, скруглений и архивной стилизации.
- Нижняя навигация и внешние инструменты лаборатории не относятся к эксперименту с шапкой и сохраняются.

**Риски:**

- У новых frame-* нельзя случайно унаследовать20px reading-gap старых экспериментов.
- Стрелка у кромки не должна пересечь первую строку заголовка; overlay не должен закрыть лицо или ключевой предмет.
- Старые метаданные/композиции и параллельные задачи защищены от изменений.

**План проверки:**

- До изменения зафиксировать frame/02 на320/390/1280px для двух экранов и обычного/длинного названия; после сравнить его сохранность и наследование новыми вариантами.
- Новые36состояний: ровно одна header-ссылка, доступное имя,≥44px, контраст/crop, отсутствие перекрытий и overflow, правильный заголовок/контент.
- Проверить переход стрелкой из отдельных экранов и iframe в существующий каталог; фокус/клавиатуру, меню12вариантов, select и longTitle.
- Build/typecheck, read-only review/маршруты, diff/whitespace/ссылки, сохранность файлов вне write set. Локальный QA, не release-pass.

## Result

**Итог:** Добавлены G / «Над кадром», H / «На картинке», I / «У кромки». В каждой новой шапке одна44×44px ссылка-стрелка в существующий редакционный каталог, без видимых «Прожито» и «Все истории». Все9старых вариантов сохранены, всего12пар. G/H/I наследуют именно исходный frame/02: прямое начертание, размеры и поля названия, crop, линия и отступы чтения не изменены.

Вход: `http://127.0.0.1:5173/prozhito/#/lab/editorial-headers/frame-nav-overlay`; соседние варианты доступны в группе «Новые · стрелка в кадре02». Рекомендация для обсуждения — H как привычный верхний возврат без отдельной полосы; это не выбор владельца и не перенос в рабочую версию.

**Файлы:** Восемь файлов объявленного write set; в PROJECT_STATE / WORKBOARD изменены только абзацы лаборатории. Другие файлы этой задачей не редактировались.

**Проверки:**

- Финальный `npm run build` / TypeScript — PASS. Прежнее предупреждение об основном bundle (~619kB) остаётся; новых зависимостей нет.
- Browser:36 новых состояний =3варианта ×2экрана ×обычный/длинный заголовок ×320/390/1280px — PASS. Фактические innerWidth совпали с заданными.
- Каждая шапка содержит ровно1ссылку с именем «К маршруту историй», правильным href/target и44×44px целью. Видимый текст шапки равен заголовку; служебные подписи не возвращаются. Непрозрачная подложка H/I сохраняет контраст на обеих иллюстрациях.
- В сравнении с baseline02 совпадают размер/начертание/выравнивание и геометрия h1 по ширине/высоте, размеры/crop изображения и32px до чтения. Стрелка не пересекается с блоком заголовка на узком/широком экране и при длинном названии.
- Исходный frame/02 повторно проверен в12состояниях: полные DOM-метрики/содержимое совпадают с исходной фиксацией. Независимый source review подтвердил побайтную сохранность9старых model-записей и CSS композиций; JSX чтения/нижней навигации и сами изображения сохранены.
- На390px первый абзац истории начинается у02≈467px, G≈459px, H/I≈403px. Разница обусловлена только верхней навигационной полосой.
- В новых вариантах один идентичный полный текст истории, один идентичный полный разбор и прежняя пара различных изображений. Второй экран сохраняет9звеньев; никакие результаты/состояния продолжения не добавлены.
- Переходы кликом из всех6отдельных preview-экранов — PASS: открывается `#/editorial-v2/`, h1«Первый месяц» и3доступные истории. Дополнительно выполнен настоящий pointer-click по видимой стрелке внутри iframeH в сравнении: весь верхний документ перешёл в каталог, iframe исчезли — PASS для `target="_top"`.
- Focus-visible — PASS: у стрелки наблюдались видимый2pxконтур/offset2px и корректное доступное имя. Клавиатурная активация — НЕ ПОДТВЕРЖДЕНА: автоматические press/keypress фокусировали ссылку, но не дали перехода по Enter; обработчиков клавиш у ссылки нет, используется нативный anchor. Это не объявляется полным keyboard-pass; нужна ручная проверка в браузере.
- Меню12ссылок/4группы проверено на320/390/768/1280px: цели≥44px, текст помещается, overflow отсутствует. Select содержит12вариантов. Проверен путь: H → длинное название → I → второй экран целиком → select G; вариант/экран/longTitle сохраняются.
- Скриншоты всех3положений на390px, I на320px и длинного названия1280px просмотрены; значимые части изображений и текст не закрыты. Console warnings/errors и framework overlay отсутствуют.
- Read-only reviewer:158 проверок маршрутов — PASS, aliases сохранены; общий LabReader неизменён. На момент review224защищённых src/content файлов совпадалиSHA256.
- Итоговый baseline281файл: вне write set параллельно изменены только3файла EditorialExercise и docs/PRODUCT.md, docs/EDITORIAL_V2.md, docs/DECISIONS.md. Они не редактировались и не откатывались этой задачей; build после параллельных обновлений проходит.
- `git diff --check`, staged check, whitespace и относительные Markdown-ссылки — PASS. Viewport сброшен к исходному, H оставлен открытым. Коммит, push и внешняя публикация не выполнялись.
- Локальный QA, не полный release-pass.

**Источники и даты:** Уточнение владельца и текущая лаборатория,2026-08-30.

**Оставшиеся риски:** Ручная проверка клавиатурной активации; назначение/расположение стрелки оценивается владельцем. Физические устройства и другие ОС вне локального QA.

**Следующий шаг:** Сравнить три положения стрелки без автоматического переноса в рабочую версию.
