# Task / Result Packet

## Task

**Название:** Упростить выбор ответа и убрать двойные мобильные отступы L01-S01

**Владелец:** Стефания (`/root`); `review_integration_contract` — read-only CSS/a11y review.

**Статус:** done

**Цель:** Убрать видимые кружки radio из ответов, сохранив native выбор и клавиатуру; растянуть цветную рабочую область упражнения на мобильном до краёв экрана, оставив один внутренний отступ. Формат упражнения и правила решения 28 не меняются.

**Входные источники:**

- [AGENTS.md](../../AGENTS.md), [PROJECT_STATE.md](../PROJECT_STATE.md), [WORKBOARD.md](../WORKBOARD.md).
- [Контракт editorial_v2](../../docs/EDITORIAL_V2.md#упражнение-l01-s01), [решение 28](../../docs/DECISIONS.md#редакционное-исключение-28), [исходный task packet](2026-08-30-editorial-v2-l01-s01-exercise.md).
- Текущая просьба владельца: подсветка контейнера уже обозначает выбор; видимые radio лишние, внешние и внутренние мобильные отступы создают избыточную вложенность.

**Write set:**

- `src/apps/editorial-v2/components/EditorialExercise/EditorialExercise.module.css`;
- `src/apps/editorial-v2/screens/EditorialSolutionScreen/EditorialSolutionScreen.module.css` — локальная величина внешнего отступа для упражнения;
- `docs/EDITORIAL_V2.md` — только визуальное уточнение;
- этот task packet.

**Вне scope:** TSX, модель вопросов, тексты, сверка, срок жизни ответов, маршруты, шапки/иллюстрации, другие Section, архив, общие токены, новые зависимости, хранение/сеть, commit/push/publication. Приоритеты и долговременный продуктовый scope не меняются, `PROJECT_STATE`/`WORKBOARD` не редактируются.

**Принятое исполнение:**

- Native radio остаётся доступным для клавиатуры и assistive technology, но визуально скрыт; вся плашка остаётся label. Колонка под кружок удаляется, клавиатурный фокус отображается на плашке.
- На ширине до 48rem только `.workspace` компенсирует внешний gutter страницы отрицательным margin-inline и теряет боковые границы; без `100vw`, JS-измерений и растягивания intro/чтения/сверки. Gutter учитывает существующее исключение 1.25rem при ширине до 35rem.
- Фон/тонкие линии и вся desktop-композиция сохраняются. Внутри мобильной рабочей области остаётся один аккуратный отступ; размер шрифта не уменьшается.

**Риски:** потеря клавиатурного фокуса при скрытии input; overflow на границах responsive-режимов; нежелательное расширение других частей экрана. Read-only review подтвердил локальный CSS-подход; существенных открытых решений нет.

**План проверки:**

- Browser/IAB: исходный и обновлённый вид, выбор всей плашки, переход/возврат, полный разбор; 320/390px, граница 560/768px и desktop.
- Native семантика и клавиатурный фокус; существующий `KeyboardControls` в Storybook без добавления зеркальных CSS-тестов.
- Сборка, отсутствие горизонтального overflow, неизменность исходного контента/TSX, относительные ссылки и diff check.

## Result

**Итог:** Видимые radio-кружки и колонка под них удалены. Выбор остаётся на всей плашке, выделяется фоном и границей толщиной 2px; клавиатурный фокус имеет отдельный внешний контур. Мобильная рабочая область растянута до краёв экрана, внутри оставлен один отступ, согласованный с обычным gutter страницы. Тексты, модель, переходы, время жизни ответов и продуктовый контракт не изменены.

**Файлы:** изменены ровно четыре файла объявленного write set: два CSS, визуальный пункт `docs/EDITORIAL_V2.md` и этот packet. TSX и общие токены не менялись.

**Проверки:**

- Browser/IAB, реальный `#/editorial-v2/section/l01-s01/analysis` — PASS: правильные адрес/title, содержимое на месте, framework overlay отсутствует, console без errors/warnings.
- Исходный вид при 390×844: workspace 350px, текст первого ответа около 270px. После правки: workspace 390px от x=0, плашка 350px, ширина текста около 326px (примерно +20%), шрифт сохранён.
- 320×720, 390×844, 560/561/768×900 — PASS: цветная область от края до края, горизонтального overflow нет. При 769 и 1280px сохранена настольная рамка/боковая навигация; overflow отсутствует. Скриншоты mobile/desktop осмотрены; на 320px длинный первый вопрос с тремя ответами доступен без обрезки.
- Клик по тексту плашки выбирает соответствующий native radio; одновременно выбран один вариант, автоматического перехода/оценки нет. Пройдено девять вопросов новым способом, сверка показывает девять совпадений; изменение и полный разбор доступны, все девять звеньев сохранены. Возврат к предыдущему вопросу сохраняет выбор. Сверка и полное чтение сохраняют обычные отступы.
- `KeyboardControls` в собранном Storybook, Browser/IAB — PASS: Tab → Space → ArrowDown → Tab → Enter. Native radio не удалён и не скрыт через `display:none`/`aria-hidden`; клавиатурный фокус выводится на всю плашку через `:focus-visible`. Автоматический axe — 0 violations / 0 inconclusive.
- `npm run build` и `npm run build-storybook -- --output-dir /tmp/prozhito-exercise-polish-storybook` — PASS. Новые тесты или зависимости для CSS-правки не добавлялись.
- Независимый read-only review итогового diff двух CSS — PASS, подтверждённых дефектов нет.
- SHA-256 baseline `src/apps/editorial-v2`, архивных `src/components`/`src/screens` и `content` — изменились только два ожидаемых CSS. Канонический контент, 6/22/48, исходные звенья и логика упражнения не затронуты.
- Относительные ссылки и `git diff --check` / `git diff --cached --check` — PASS.

**Источники и дата:** явное уточнение владельца и реальный UI проверены 2026-08-30. Новых финансовых тезисов нет.

**Артефакты:** `/tmp/prozhito-exercise-polish-mobile.jpg`, `/tmp/prozhito-exercise-polish-build.log`, `/tmp/prozhito-exercise-polish-storybook.log`. Временный сервер Storybook остановлен, существующая демоверсия остаётся доступна.

**Оставшиеся ограничения:** проверены эмулированные размеры, а не физические устройства; VoiceOver/forced-colors в браузере отдельно не прогонялись. Существующее предупреждение сборки о размере общего bundle не относится к CSS-правке. Полный release-pass не заявляется. Commit, push и внешняя публикация не выполнялись.

**Следующий шаг:** владелец оценивает обновлённый [второй экран](http://127.0.0.1:5173/prozhito/#/editorial-v2/section/l01-s01/analysis); дальнейшая визуальная итерация — по отдельному уточнению.
