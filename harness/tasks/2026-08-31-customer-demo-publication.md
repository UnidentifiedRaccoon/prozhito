# Task / Result Packet

## Task

**Название:** Публикация текущей демоверсии для просмотра заказчиком

**Владелец:** Стефания

**Статус:** active

**Цель:** Опубликовать актуальную демоверсию на существующем GitHub Pages и проверить три адреса: разводящая, архив, editorial_v2.

**Входные источники:**

- Явный запрос владельца от 31 августа 2026 года на публикацию демоверсии на GitHub и три ссылки для заказчика.
- [Состояние проекта](../PROJECT_STATE.md), [risk policy](../RISK_POLICY.md), [решения](../../docs/DECISIONS.md), [editorial_v2](../../docs/EDITORIAL_V2.md).
- Проверенный target: `UnidentifiedRaccoon/prozhito`, PUBLIC, default branch `main`; действующий GitHub account — `UnidentifiedRaccoon`; GitHub Pages через существующий workflow, адрес `https://unidentifiedraccoon.github.io/prozhito/`.

**Write set:**

- Этот task/result packet, `README.md`, `harness/PROJECT_STATE.md`.
- Зафиксировать накопленные проектные изменения в `AGENTS.md`, `docs/`, `harness/`, `src/`: новая редакционная версия, разводящая, их ресурсы/документация и существующие локальные лаборатории, от которых зависит текущая сборка. Точный staged set проверяется перед коммитом.
- Обычный коммит и fast-forward push в `main` существующего репозитория, запускающий действующий Pages workflow.

**Вне scope:**

- Новый репозиторий, смена видимости/доступа, workflow/инфраструктура, backend, зависимости, новые финансовые тезисы и изменения канонических Section.
- Новые ссылки на лаборатории в пользовательской разводящей; письма и сообщения заказчику; destructive Git, force-push.

**Риски:**

- Репозиторий и сайт публичные; проверяются staged diff, запрещённые файлы, секреты и самостоятельная история Git.
- Hash-маршруты требуют проверки рендера в браузере, одного HTTP 200 недостаточно.
- Публикация содержит накопленные изменения нескольких принятых задач; нельзя ограничиться только последними двумя изображениями.

**План проверки:**

- Проверить состояние remote, список файлов и содержимое изменений; скан секретов без вывода найденных значений.
- `git diff --check`, `git diff --cached --check`, относительные ссылки, 6/22, распределение 3/4/4/4/4/3, 48 действий, девять звеньев каждой Section.
- Production build и Storybook build; выборочный browser smoke трех entrypoint.
- Успешный Pages workflow для точного опубликованного SHA, проверка трёх публичных маршрутов и загрузки изображений.

## Result

В работе.
