# Task / Result Packet

## Task

**Название:** Добавить курсивный разворот и два кадра с двухстрочным заголовком

**Владелец:** Стефания; centered_frame_review — read-only review.

**Статус:** done

**Цель:** Сохранить все шесть текущих вариантов и добавить три новых: курсивный разворот, курсивный кадр с центрированным заголовком в две строки и такой же кадр со штрихом.

**Входные источники:**

- AGENTS.md, PROJECT_STATE.md, WORKBOARD.md, TASK_TEMPLATE.md, docs/EDITORIAL_V2.md.
- [Предыдущая итерация](2026-08-30-editorial-header-lab-centered-frame.md), текущая лаборатория и уточнение владельца от2026-08-30.
- Ранее исследованные редакционные референсы и проектная пара изображений. Это точечная итерация существующих композиций; новые изображения и генерация концепта не нужны. Применяются ранее прочитанные Frontend App Builder / Frontend Testing / Browser. Impeccable недоступен и не используется.

**Write set:**

- src/apps/editorial-header-lab/model.ts.
- src/apps/editorial-header-lab/HeaderComposition.tsx.
- src/apps/editorial-header-lab/HeaderLab.module.css.
- src/apps/editorial-header-lab/HeaderLabApp.tsx.
- src/apps/editorial-header-lab/README.md.
- Этот packet; только абзацы лаборатории в harness/PROJECT_STATE.md и harness/WORKBOARD.md.

**Вне scope:**

- Удаление или изменение шести имеющихся шапок и прежних alias-маршрутов.
- Канон, изображения, число экранов, девять звеньев и поведение второго экрана.
- Production editorial_v2, упражнение, архив, зависимости, коммит, push, публикация, автоматический выбор победителя.

**Дизайн-контракт:**

- D / split-italic / «Разворот курсив»: исходный split с курсивом заголовка, прежней геометрией и навигацией.
- E / frame-italic-two-line / «Кадр в 2 строки»: общий frameStudy, курсив, центр, ограничение ширины h1 в ch для двух строк обычного названия L01-S01.
- F / frame-rule-italic-two-line / «2 строки и штрих»: та же ширина, размер и начертание, плюс прежний центральный штрих72×3px. Штрих не влияет на высоту/перенос текста.
- Не вставлять обязательные переносы или nowrap в канонический текст, не обрезать строки. Длинный тестовый заголовок естественно занимает больше двух строк.
- Все шесть прежних вариантов остаются в переключателе под теми же ID/названиями. Новая тройка — отдельная группа. Меню не должно закрывать чтение на телефоне.

**Риски:**

- Курсив в узкой колонке разворота: проверить край букв и overflow320px.
- Геометрия новых кадров не должна менять старые A/C.
- Проверить двухстрочность на320/390/1280px и обоих экранах; на других системных гарнитурах переносы могут отличаться.
- Рабочее дерево содержит параллельную работу вне лаборатории: не откатывать её.

**План проверки:**

- До изменений зафиксированы36 Browser-состояний существующих шести пар на320/390/1280px; после сравнить геометрию, начертание, изображения и тексты.
- Новые пары: те же три ширины, обычный/длинный заголовок, количество строк, читаемость, crop, навигация≥44px и отсутствие overflow.
- Меню9пунктов, select, переключение вариантов/longTitle/ширины и переход между экранами.
- Build/typecheck, маршруты и read-only review, diff/whitespace/ссылки, сохранность защищённых файлов. Локальный QA, не release-pass.

## Result

**Итог:** Добавлены D / «Разворот курсив», E / «Кадр в 2 строки», F / «2 строки и штрих». Все шесть прежних вариантов сохранены, всего9пар. У E/F заголовок центрирован, курсивный, шириной≤13ch; обычное название L01-S01 занимает ровно2строки на обоих экранах. Штрих не влияет на геометрию текста.

Локальный вход: `http://127.0.0.1:5173/prozhito/#/lab/editorial-headers/frame-italic-two-line`. D/F доступны в группе новых вариантов и по своим ID из дизайн-контракта.

**Файлы:** Восемь файлов объявленного write set. Общие документы изменены только в абзацах лаборатории. Прежние packets не переписывались.

**Проверки:**

- Финальный `npm run build` / TypeScript — PASS; прежнее предупреждение о большом bundle (~617kB) остаётся. Новых зависимостей нет.
- Browser:36 новых состояний =3варианта ×2экрана ×обычный/длинный заголовок ×320/390/1280px — PASS. Фактические viewport проверены.
- E/F: во всех12сочетаниях обычного названия (2варианта ×2экрана ×3ширины) отношение высоты h1 к line-height равно2. Размер шрифта и центрирование сохранены. E/F имеют идентичную геометрию изображения/заголовка и одинаковое начало чтения, включая длинные названия.
- На390px первый абзац у D≈388px, у E/F≈394.6px. Новая двухстрочная шапка ниже A/C ровно на одну строку; жёстких переносов и обрезки нет.
- Проверены читаемость и crop: курсивный разворот320px, двухстрочный кадр390px, кадр со штрихом320/1280px, длинные названия и обе роли изображений. Пересечения с содержимым, framework overlay и overflow отсутствуют; цели навигации≥44px, один h1, девять звеньев второго экрана.
- DOM: единый полный текст истории и единый разбор во всех новых вариантах, ровно2разных изображения. Канонические файлы и рабочие компоненты не менялись.
- Повторно проверены36 состояний старых6вариантов: без runtime ошибок, пропавших изображений или overflow. Первичная координатная фиксация не использована как pixel-diff: фактический innerWidth тогда отличался от заданного (291/354/1164 вместо320/390/1280). Сохранность старых композиций доказана отдельным сравнением исходников, а не некорректным сравнением разных viewport.
- Независимый read-only review: первые6записей model и CSS всех6старых шапок побайтно сохранены; JSX изображения/чтения/нижней навигации идентичен.122 комбинации маршрутов и прежние aliases — PASS.
- Общее меню:9ссылок в3группах на320/390/768/1280px, без переполнения, ссылки≥44px. На узком экране меню не липнет и не закрывает чтение. В отдельном reader расширен только select до174px: полные названия D/F помещаются, горизонтального overflow нет.
- Interaction loop: F → второй экран целиком → select D → лаборатория → длинный заголовок → E → обычное название → режим«Шире». Состояния экрана и longTitle сохраняются; режим«Шире» даёт≈567px каждой колонке на1280px.
- Console warnings/errors отсутствуют. Viewport сброшен к исходному; лаборатория оставлена открытой наE.
- Baseline280 файлов: src/content/production/assets сохранены. Вне write set параллельно обновился только packet `2026-08-30-editorial-exercise-step-states.md`; он не редактировался и не откатывался этой задачей.
- Whitespace и относительные Markdown-ссылки — PASS; `git diff --check` / staged check — PASS. Коммит, push и публикация не выполнялись.
- Это локальная проверка, не release-pass; новый концепт не объявлялся утверждённым владельцем.

**Источники и даты:** Уточнение владельца и текущая лаборатория,2026-08-30.

**Оставшиеся риски:** Различия системных гарнитур и физические устройства за пределами локального Browser QA.

**Следующий шаг:** Сравнение новых трёх пар владельцем; без переноса в рабочую версию.
