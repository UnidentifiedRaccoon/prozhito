# Task / Result Packet

## Task

**Название:** Пункты десктопного меню до краёв колонки

**Владелец:** Стефания (`/root`); `audit_header_navigation` — read-only review.

**Статус:** done

**Цель:** Убрать внешний padding у десктопного меню «Звенья цепочки», сохранив внутренний отступ текста и всю ширину интерактивных пунктов.

**Входные источники:** просьба и скриншот владельца от 30 августа 2026 года; [AGENTS.md](../../AGENTS.md), [PROJECT_STATE.md](../PROJECT_STATE.md), [локальный UI-контракт](../../docs/EDITORIAL_V2.md#упражнение-l01-s01), [предыдущая итерация ленты](2026-08-30-editorial-exercise-mobile-rail.md).

**Write set:**

- `src/apps/editorial-v2/components/EditorialExercise/EditorialExercise.module.css`;
- `docs/EDITORIAL_V2.md` — уточнение только десктопного вида меню;
- этот packet.

**Вне scope:** логика упражнения, контент, шапки, другие Section, архив, мобильная лента и её прокрутка, новые зависимости/данные/хранение, commit/push/publication.

**Риски:** потеря отступа вокруг текста, обрезанный focus/active border, изменение мобильных размеров из-за CSS cascade.

**План проверки:** измерить края nav и кнопок на desktop; проверить состояния и переход; сравнить mobile до/после; build, diff, ссылки и baseline исходников. Новые тесты поведения не нужны: JSX и state не меняются.

## Result

**Итог:** внешний padding десктопного nav убран; пункты, их заливки, активная рамка и разделители занимают всю ширину колонки. Горизонтальный отступ текста перенесён внутрь кнопок (`1.25rem`). Видимый клавиатурный focus направлен внутрь строки (`outline-offset:-3px`), с `Highlight` в forced-colors. Мобильные overrides сохранены. Обновлены только три файла write set; пользовательский контракт и логика не меняются.

**Проверки:**

- Browser1280×900: nav240px, каждый из девяти пунктов240px, боковые зазоры0px (до изменения:216px и12px с каждой стороны). Заливки отвеченного/текущего/недоступного различимы; выбранный ответ сохранён при переходе вперёд/назад. Скриншот визуально проверен: `/tmp/prozhito-exercise-desktop-rail-1280.jpg`.
- Browser769×900: все пункты равны ширине колонки, padding0, переполнения страницы нет.
- Browser390×844: измерения до/после идентичны — nav390px/padding0, кнопки184×72px/padding13.6px20px, горизонтальный overflow:auto, scrollbar:none, разделитель24px. Мобильное оформление не изменилось.
- Console error/warn —0. Production build с TypeScript — PASS; прежнее предупреждение о chunk больше500kB сохраняется.
- Read-only review — существенных замечаний нет; рекомендация внутреннего фокуса учтена. `git diff --check`, `git diff --cached --check`, whitespace и13 относительных ссылок — PASS.
- Baseline234 файлов `src/` и `content/`: изменён только CSS этого компонента. JSX, Stories, канонический контент, архив и остальные Section неизменны.

**Оставшиеся риски:** локальный UI-check, не полный release-pass. Полный набор поведенческих Stories и физические устройства не перепроверялись для этой CSS-правки; результат предыдущей итерации сохранён в её packet. Forced-colors/focus проверены по CSS без отдельной эмуляции.

**Следующий шаг:** оценка владельцем текущего десктопного вида; следующие Section и публикация не запускаются.
