# Task / Result Packet

## Task

**Название:** Три минималистичных вариации «Кадра»

**Владелец:** Стефания; design_references — read-only дизайн-разбор и review.

**Статус:** done

**Цель:** Упростить лабораторию до трёх оставленных основ (Разворот, Кадр, Врезка), добавить три разных вариации исходного «Кадра» с более плотным переходом к чтению.

**Входные источники:**

- AGENTS.md, harness/PROJECT_STATE.md, harness/WORKBOARD.md, harness/TASK_TEMPLATE.md.
- Предыдущие packets editorial-header-lab, light-overlays и gradient от2026-08-30; текущий исходный frame в браузере.
- Уточнение владельца: «Кадр» нравится больше, но слишком большие отступы и недостаточно характера; удалить прочие направления из лаборатории.
- Ранее исследованные Queue/Pentagram, Longreads, Pinterest и Pudding — ритм наборной полосы, разные отношения изображения и заголовка, современная типографика. Новые растровые материалы не нужны.

**Write set:**

- src/apps/editorial-header-lab/HeaderComposition.tsx.
- src/apps/editorial-header-lab/HeaderLab.module.css.
- src/apps/editorial-header-lab/HeaderLabApp.tsx.
- src/apps/editorial-header-lab/model.ts.
- src/apps/editorial-header-lab/README.md.
- Этот packet, harness/PROJECT_STATE.md, harness/WORKBOARD.md — только состояние лаборатории.

**Вне scope:**

- Канонический content, рабочая editorial_v2 и её упражнение L01-S01, архив, изображения, девять звеньев и поведение второго экрана лаборатории.
- Переделка оставленных split/frame/inset: это контрольные основы. Их header/reading-композиция сохраняется.
- Генерация новых изображений, выбор победителя, автоматический перенос, commit, push, внешняя публикация.

**Дизайн-направления:**

- В исходном frame заголовок имеет двойные боковые поля, а после него складывается62px пустого пространства. Для новых вариантов один gutter, компактные интервалы12–20px, название в нормальном потоке.
- A frame-edge / «Кромка»: image-first, навигация сверху, крупная антиква слева на оси чтения, короткий графический штрих у края картинки.
- B frame-caption / «Подпись»: изображение в полях сохраняет полный3:2, плотный sans-заголовок под ним, навигация единственной строкой ниже названия.
- C frame-folio / «Фолио»: широкое изображение и асимметричная полоса «возврат слева / курсивный заголовок справа»; мобильный возврат компактный, с доступным именем и44px целью.
- Одна Section L01-S01, прежняя пара разных изображений и равные тексты. Никаких меток «История01 / Решение01».
- Списки основ и новых вариантов визуально разделены. Удалённые title/margin/inset-light/frame-veil больше не рендерятся; старые входящие маршруты ведут на исходный frame, чтобы текущая открытая вкладка не сломалась.

**Риски:**

- Курсив и узкий заголовок C должны оставаться читаемыми на320px.
- Обрезка широких изображений не должна убирать лицо/телефон. B намеренно показывает полный кадр меньшим масштабом.
- Параллельная разработка других частей проекта не входит в write set; проверять изменения baseline и не откатывать их.
- В новом составе остаётся шесть сравнительных пар, но у каждой Section по-прежнему только два экрана.

**План проверки:**

- Build/typecheck, маршруты6вариантов и fallback старых ссылок, один h1, полное одинаковое чтение/девять звеньев.
- Browser/IAB: три новые пары320/390/1280px, обычные и длинные заголовки, crop/контраст/переход к чтению, цели навигации и overflow.
- Проверить три сохранённые основы и упрощённое меню, переключение/открытие отдельного экрана.
- Whitespace, относительные ссылки и сохранность вне write set; локальный QA, не release-pass.

## Result

**Итог:** В лаборатории шесть пар: сохранены Разворот/Кадр/Врезка, добавлены A/Кромка, B/Подпись и C/Фолио. Меню разделено на две группы. Прочие композиции удалены из реализации и переключателей; прежние входящие ссылки открывают исходный кадр. Рабочая editorial_v2 и её упражнение не затронуты.

Локальные адреса: `http://127.0.0.1:5173/prozhito/#/lab/editorial-headers/frame-edge`, `frame-caption`, `frame-folio` (последние два — значение варианта в том же маршруте). Общий dev-server снова исправен; временный порт4174 относится к предыдущему историческому снимку и для этой итерации не используется.

**Файлы:** Восемь файлов объявленного write set: четыре файла реализации, README лаборатории, этот packet и только абзацы лаборатории в PROJECT_STATE/WORKBOARD.

**Проверки:**

- `npm run build` и TypeScript — PASS после финальных изменений. Прежнее предупреждение о большом основном bundle (~616kB) остаётся.
- Server-render:48 сочетаний6вариантов ×2экранов ×preview/standalone ×обычный/длинный заголовок. Один h1, без меток типа экрана, идентичное полное чтение и одинаковая пара разных изображений — PASS.
- Старые4варианта больше не рендерятся;8 тестовых retired reader-адресов сохраняют тип экрана, preview и длинное название, открывая frame. Пустой вход тоже frame, неизвестный ID не принимается — PASS.
- Контент:6уровней,22Section, распределение3/4/4/4/4/3,48действий,9звеньев в каждой Section — PASS.
- Browser/IAB:36 состояний новых пар на320/390/1280px с обычным и длинным названием; ещё12 состояний сохранённых основ на320/1280. Все картинки загружаются, заголовки не выходят за viewport, содержание ниже h1, видимые ссылки не меньше44×44px, overflow отсутствует, второй экран сохраняет9звеньев — PASS.
- Визуально просмотрены все три направления, обе роли изображений, широкие и узкие экраны, длинный курсив C и полный кадр B. Для A/C уточнён story-focal: лицо остаётся целиком, телефон различим. В B исходное3:2 показано без обрезки.
- В независимом read-only review найдено несовпадение оси заголовка A с историей наdesktop. Исправлено ограничением titleGroup по42rem колонке; браузер1280px подтвердил x≈304px у h1 и первого абзаца. Штрих выровнен на ту же ось.
- На390px первый абзац истории: исходный frame≈467px; A≈394px, B≈376px, C≈349px. Это73/91/118px более раннее начало чтения на этой Section; не универсальное обещание для любого названия.
- Меню320/768/1280:6ссылок в2группах, текст кнопок помещается, горизонтальной прокрутки нет. Проверены A→C с сохранением длинного названия, его включение/выключение, режим«Шире», открытие второго экрана и выбор B через select — PASS.
- Console warnings/errors и framework overlay в финальном просмотре не обнаружены.
- 268 файлов baseline: вне write set параллельно обновились только docs/EDITORIAL_V2.md и packet упражнения L01-S01. Они не изменялись этой задачей и не откатывались; остальные защищённые файлы совпадают, в том числе канон, рабочие экраны, изображения, компоненты чтения, App и router.
- `git diff --check`, staged check, whitespace новых файлов и относительные ссылки — PASS. Коммит, push и публикация не выполнялись.
- Проверка локальная, не полный release-pass.

**Источники и даты:** Уточнение владельца, исходный frame и прежние референсы,2026-08-30.

**Оставшиеся риски:** Owner review и реальные мобильные устройства — отдельный шаг. Курсив C зависит от доступного системного serif; A/C требуют подходящего широкого crop, B уменьшает масштаб сцены на телефоне. Варианты не перенесены в рабочую версию.

**Следующий шаг:** Сравнить три новые интонации с исходным кадром; ничего автоматически не переносить.
