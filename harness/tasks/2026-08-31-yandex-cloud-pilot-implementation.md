# Task / Result Packet

## Task

**Название:** Отдельный рабочий пилот «Прожито» в Yandex Cloud.

**Владелец:** Стефания.

**Статус:** pilot-deployed; awaiting owner approval for live daytime stop/start test

**Цель:** Проверенный открытый веб-контур без аккаунтов читателей, с отдельной админкой и аккаунтом редактора, изменением и публикацией контента, PostgreSQL и ночным режимом 00:00–08:00 МСК.

**Входные источники:**

- Явное разрешение владельца от 31 августа 2026 года: без месячного лимита; админка и вход на запуске; без прогресса; около 100 пользователей в день; технический адрес допустим; создать новый каталог.
- Последующее уточнение [решения 39](../../docs/DECISIONS.md#открытый-сайт-39): публичный сайт не требует никаких аккаунтов; вход только редактору в отдельную админку. Блокер выдачи читательских аккаунтов снят, реализация возобновлена.
- [Решение 37](../../docs/DECISIONS.md#тестовая-инфраструктура-37), [согласованное направление](../../docs/YANDEX_CLOUD_INFRASTRUCTURE_PROPOSAL.md).
- [Продукт](../../docs/PRODUCT.md), [состояние](../PROJECT_STATE.md), [риски](../RISK_POLICY.md), текущие клиентские контракты.
- FinPulse используется только как read-only референс; исходный каталог `b1gpl04msqva2tsff46k`, облако `b1gmnnjgdcc1b7qhjmlk`.

**Write set:**

- Этот packet; `docs/DECISIONS.md`, `docs/PRODUCT.md`, `docs/YANDEX_CLOUD_INFRASTRUCTURE_PROPOSAL.md`, `docs/operations/`, `AGENTS.md`, `harness/PROJECT_STATE.md` для фактического нового состояния.
- Новые `server/`, `infra/yandex-cloud/`, `scripts/`, тесты и необходимые конфигурации сборки; `package.json`, lockfile, `.gitignore`, отдельная cloud-сборка клиента.
- `src/apps/cloud/`, отдельная `admin/`, точечная интеграция runtime-контента в существующую оболочку. Вход только в admin. Тексты и изображения не редактируются.
- Новый каталог Yandex Cloud проекта; по разрешению использовать найденный домен — только новые свободные поддомены `prozhito.ncfg.ru`, `prozhito-editor.ncfg.ru` и их ACME-записи. Права и ссылки на секреты — без значений в репозитории/выводе.

**Вне scope:**

- Изменение FinPulse, ресурсов других каталогов, действующего GitHub Pages, чужих DNS-записей.
- Аккаунты/регистрация/сессии читателей, прогресс, личные ответы, аналитика, платежи, новая методическая редактура, расширение упражнения на остальные Section.
- Коммит/push несвязанных накопленных изменений; автоматическая замена канонических Markdown содержимым БД.
- Месячный денежный лимит и второй постоянно работающий контур.

**Риски:**

- Создание ресурсов тарифицируется; ночная остановка не прекращает оплату дисков и копий. Один хост — без отказоустойчивости.
- HTTPS обязателен для входа; найденные чужие домены нельзя перенастраивать без проверки назначения.
- Регистрация/сессии не должны записывать прогресс. Учётные данные и секреты не выводить.
- Реальная готовность БД после 07:45 не гарантирована к 08:00: UI остаётся закрыт до readiness.
- В рабочем дереве много предшествующих изменений; сохранить их без присвоения и перезаписи.

**План проверки:**

- Проверить облако/каталог/доступные DNS и сервисы до платного развёртывания.
- Сборка/типизация; unit/integration проверки авторизации, CSRF, валидации, конфликтов, публикации и расписания.
- Браузер: вход, редакторский черновик/публикация, reader, мобильный экран, технические работы.
- Закрытая БД/TLS, отсутствие анонимного доступа к backend, первый backup и восстановление, идемпотентность контроллера.
- Сверить импорт 6 / 22 / 48 и девять звеньев, неизменность исходников; `git diff --check`.

## Result

**Итог:** Развёрнут отдельный пилот в каталоге `prozhito` / `b1gr37s9qf8nh0fuq1js`, облако `b1gmnnjgdcc1b7qhjmlk`. [Сайт](https://prozhito.ncfg.ru/) открыт без аккаунтов читателей; [отдельная админка](https://prozhito-editor.ncfg.ru/) имеет одного редактора, пароль и TOTP. Оба HTTPS-адреса проверены, сертификат `fpqnl7n8j5epu5t3f9km` ISSUED. Добавлены только четыре новые DNS-записи проекта.

Созданы закрытая PostgreSQL 17 `c9qgfensmmafe5c70nhm` (`s3-c2-m8`, 10 GiB, 14 дней копий), отдельная VPC, приватный API, три закрытых бакета, два шлюза, Lockbox и ночной таймер. Импортированы 6 уровней, 22 Section и 50 медиа. Контроллер ограничен конкретным кластером, не каталогом. FinPulse и GitHub Pages не менялись. ID и digest образа — [pilot.json](../../infra/yandex-cloud/pilot.json).

**Файлы:** `server/`, `admin/`, `src/apps/cloud/`, `src/content/cloudContent.tsx`, точечные адаптеры экранов, конфигурации сборки, `infra/yandex-cloud/`, документы решений/продукта/состояния и [инструкция эксплуатации](../../docs/operations/yandex-cloud-pilot.md). Предшествующие изменения сохранены; тексты и иллюстрации в этой задаче не редактировались. Коммит/push накопленного worktree не выполнялся.

**Проверки:**

- Сборки обычного демо, cloud, admin и серверная типизация — PASS. Cloud reader отделён от лабораторий и встроенного Markdown; основной JS уменьшен примерно с 758 до 466 kB до gzip.
- Локальная изолированная PostgreSQL 17: все 4 теста PASS, включая миграции, запрет повторного seed, ограничения DDL/истории, анонимное чтение без cookie, auth/MFA/replay, CSRF, stale write 409, черновик, publish/rollback/discard и ошибки медиа/кадрирования. Контроллер — 2 теста PASS; московские границы не зависят от timezone хоста.
- Облако: `verify-public.mjs` PASS — 6/22 и полное равенство импорту, все 50 изображений, reader без cookie, admin route на reader 404, editor без сессии 401, прямой backend и бакет 403.
- `cloud-auth-smoke.mjs` PASS на техническом и собственном доменах: вход с MFA, Secure/HttpOnly/SameSite cookie, CSRF-отказ, чтение черновика, logout и отзыв сессии. Секреты только в памяти процесса, облачный контент не менялся.
- Браузер: облачный каталог и история без входа; локальная админка — вход, сохранение, подтверждение публикации, история, откат и reader-preview. Каталог и редактор проверены на ширине 390px без горизонтального переполнения. Неподдерживаемый системный prompt заменён встроенной формой подтверждения.
- Ночной интерфейс проверен в локальном изолированном приложении с тестовым временем 00:05 МСК: заголовок технических работ и оба интервала 00:00–08:00 / 08:00–00:00 показаны; консоль без ошибок. Эта проверка не останавливала рабочую БД.
- Backup/restore — PASS. Копия `c9qgfensmmafe5c70nhm:mdbtp13sv6ng18q7eoe5` восстановлена в закрытый `c9qim97f7sm54su25njg`; проверены 6/22, один редактор и полное совпадение документов с seed. Временный кластер удалён (операция `c9qng1un8uv92srhptt4` завершена), bootstrap container `bbajv4uei1sn0vc6sgmn` удалён, доступ его SA к начальному паролю отозван.
- Таймер `a1so8nv1uiaae3ismuka` активен. Приватный дневной вызов controller вернул `running/RUNNING`; ночной status по тесту не обращается к БД. **Реальный stop/start рабочего кластера не выполнен:** дневной тест отклонён проверкой полномочий, поскольку согласовано ночное окно. Команда не исполнялась, обхода не было; владельцу задан отдельный вопрос. БД RUNNING / ALIVE.
- `git diff --check`, `git diff --cached --check` — PASS на текущем этапе. Это техническая проверка пилота, не полный методический release-pass.
- После последнего выпуска admin HTML и все JS/CSS проверены обычными GET (PASS; HEAD для этих маршрутов шлюза не настроен). Повторный public smoke — PASS; оба домена HTTPS 200, БД RUNNING / ALIVE, таймер ACTIVE. Редакционный каталог → история → решение → полный разбор без упражнения также проверены без входа.
- Локальные тестовые серверы остановлены; созданный для тестов Docker-контейнер с синтетической PostgreSQL удалён при остановке. Временные браузерные QA-снимки удалены после сверки; опубликованный сайт, его данные и резервная копия сохранены.

**Источники и даты:** Локальные документы и Yandex Cloud API, 31 августа 2026 года.

**Оставшиеся риски:** Первый реальный ночной цикл / отдельное разрешение на дневной тест; передача редактору пароля и TOTP из Lockbox владельцем; эксплуатационные оповещения и бюджет. Новые media-upload, управление несколькими редакторами, Terraform и GitHub OIDC/cloud-deploy не подключены; организационный cutover Markdown не объявлен. Внешняя KB не настроена (`active: []`), использованы проектные документы; инициализация не выполнялась.

**Следующий шаг:** Получить ответ владельца о кратком дневном stop/start. Без разрешения БД вне ночного окна не останавливать; расписание уже действует. Передать редакторский доступ по инструкции, затем отдельно согласовать Git scope и остальные пункты автоматизации.

### Визуальная проверка редактора

Применён `frontend-app-builder`: концепт до реализации, затем сравнение с работающим интерфейсом. [Исходный концепт](/Users/yura-posledov/.codex/generated_images/01a05820-77b2-7ec0-a965-26beedcfbec0/exec-77c319ea-3b99-485b-8e30-5712c1f019c0.png). Снимки сделаны Browser/IAB; концепт и последний снимок непосредственно просмотрены через `view_image` в одном QA-проходе. Проверены исходные 1505×1045 и мобильные 390×844; ширина документа равна viewport, основные кнопки целиком видны в первом desktop-экране. Временные снимки не входят в репозиторий.

| Проверка | Концепт / наблюдение | Исправление или намеренное отличие |
|---|---|---|
| Компоновка | Шапка, левая колонка около 384px, открытая рабочая область | Ширина колонки и отступ 48px приведены к концепту; лишних карточек нет |
| Типографика | Крупные засечковые заголовки, читаемые нейтральные элементы формы | Заголовок 48px, элементы desktop-формы 20px; устранён слишком мелкий UI |
| Палитра | Почти белое поле, светлая боковая колонка, сине-серый акцент | Убрано избыточное кремовое тонирование; без градиентов и декоративных изображений |
| Плотность и кнопки | Поле истории и действия в первом desktop-экране | Убраны лишний grid-gap и постоянно раскрытое описание каталога; низ кнопок 1032px при высоте 1045px |
| Тексты первого экрана | Навигация и действия сохранены | Вымышленный текст концепта заменён каноническим, названия Section и номер публикации фактические. Добавлено сворачиваемое «Описание для каталога», необходимое для редактирования существующего поля |
| Адаптивность | Desktop-концепт задаёт основной порядок | На телефоне колонка становится верхним списком; собственная прокрутка вкладок, без переполнения страницы |
| Состояния и взаимодействия | Сохранение, публикация и история — настоящие элементы | Неактивные кнопки до изменения/сохранения, защищённый вход и встроенные подтверждения — необходимые рабочие состояния. Вход → черновик → публикация → история → откат и предпросмотр проверены локально |

Визуальная структура сверена с концептом; существенных неисправленных расхождений не осталось. Намеренные отличия перечислены выше, исходный пользовательский контент ради макета не менялся. Проверка функциональности и визуальная сверка выполнены отдельно.
