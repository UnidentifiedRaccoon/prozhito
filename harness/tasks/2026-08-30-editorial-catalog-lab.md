# Task / Result Packet

## Task

**Название:** Отдельная лаборатория компактного мобильного каталога.

**Владелец:** root — страница, маршруты и интеграция; audit_header_navigation — только `CatalogCard.tsx` и `CatalogCard.module.css`; независимое read-only ревью — review_integration_contract.

**Статус:** implemented — лаборатория готова; browser/mobile QA ограничен сессией, выбор варианта открыт.

**Цель:** Рабочая локальная страница с исходной мобильной карточкой и тремя компактными композициями для всех трёх Section editorial_v2. Пользователь сравнивает варианты и открывает настоящие истории. Основной каталог не меняется; ни один эксперимент не объявляется принятым.

**Основание:** запрос и скриншот владельца 30.08.2026: текст слева / картинка справа, фоновая картинка, наплывающее текстовое поле; экономия вертикального пространства.

**Входные источники:** `AGENTS.md`, `harness/PROJECT_STATE.md`, `harness/WORKBOARD.md`, `docs/PRODUCT.md`, `docs/DECISIONS.md`, `docs/EDITORIAL_V2.md`; текущие `EditorialChapterIndex`, `EditorialArtwork`, `EditorialTheme`, program/visuals трёх Section и отдельная лаборатория шапок как пример изоляции маршрута. Skills: `frontend-app-builder`, `imagegen`.

**Write set:**

- `src/apps/editorial-catalog-lab/model.ts`.
- `src/apps/editorial-catalog-lab/CatalogLabApp.tsx`.
- `src/apps/editorial-catalog-lab/CatalogLab.module.css`.
- `src/apps/editorial-catalog-lab/CatalogCard.tsx` — только audit_header_navigation.
- `src/apps/editorial-catalog-lab/CatalogCard.module.css` — только audit_header_navigation.
- `src/apps/editorial-catalog-lab/CatalogLab.stories.tsx`.
- `src/apps/editorial-catalog-lab/README.md`.
- `src/router.ts`, `src/App.tsx` — только регистрация изолированного lazy-маршрута.
- `docs/EDITORIAL_V2.md`, `harness/PROJECT_STATE.md`, `harness/WORKBOARD.md`, этот packet — только root.
- Временные baseline, проверки и сборки: `/tmp/prozhito-catalog-lab-*`.

**Вне scope:** рабочий каталог и экраны Section, упражнения, архив, прежние лаборатории, канонический контент, изображения и production focal metadata, сервер/хранилище/аналитика, зависимости, коммит/push/публикация. На рабочий каталог не добавляется лабораторная навигация.

**Композиции и дизайн-инвентарь:**

- 0 «Текущий»: мобильная анатомия исходной карточки, номер в отдельной колонке, большой заголовок, подпись, CTA, изображение ниже 3:2. Это эталон представления на узком экране, без правки production-компонента.
- A «Сплит»: текст слева, вертикальный кадр справа; номер не отнимает отдельную колонку у подписи. Высота определяется полным текстом.
- B «Фон»: кадр справа уходит в бумагу; под всей областью текста почти непрозрачный `#FCFCFB`, не графитовый текст на пёстром изображении.
- C «Наплыв»: текстовое поле наезжает на нижний левый край более широкого кадра. Полоса картинки сохраняет портрет и контекст; её высота не используется для обещания точного процента экономии.
- Все варианты: три существующих title/summary, номера 01–03, CTA «Открыть историю» и одна нативная ссылка на строку; нынешние serif/sans, `#FCFCFB`, `#202522`, `#66706A`, линии `#CFD5D1`, акцент `#245D65`; без теней, плашек, скруглённых карточек и новых сюжетных assets. Копия не обрезается и не сокращается. В компактных пробах title около26–28px, summary14px, CTA12–13px; без фиксированной высоты текста.
- Lab chrome: заголовок, ссылка в рабочий каталог, четыре варианта и общий режим сравнения; на desktop выбор максимальной ширины пробы320/390/430px, на mobile естественная доступная ширина. Никакого сохранения предпочтений.
- Генеративный эскиз композиций (preview only): `/Users/yura-posledov/.codex/generated_images/01a051c4-9fa0-7882-9612-f3e495fa2888/exec-39733192-09a2-40d8-8178-8bd16552f64e.png`. Вызван built-in imagegen; исходный скриншот — reference, не редактируемый продуктовый asset. Основной brief: три полные мобильные карточки A split / B fade into paper / C overlap, точная копия первой Section и текущая палитра. Это не утверждённый дизайн и не UI-растр: изображение не подключается к приложению, используются настоящие assets и HTML. Намеренные отличия от эскиза: убрать его лишнее пустое пространство перед CTA и не воспроизводить перерисованные детали иллюстраций.

**Риски и границы:**

- Узкий кадр может потерять жест Саши или черновик; локальные lab-focal выбираются отдельно, production metadata не меняются.
- Тёмная иллюстрация S03 требует плотной подложки под текстом фонового варианта.
- Лабораторная ширина не должна зависеть от ширины desktop viewport; использовать container sizing, без scale и iframe.
- Браузер заблокирован ранее полученным URL-policy ограничением; обход через другой браузер, порт или HTTP не допускается. По skill frontend-app-builder требуется сравнение с реальным скриншотом; эта часть остаётся явным ограничением, а не основанием объявить визуальное совпадение или принятие вариантов.

**План проверки:** маршруты и прямые ссылки/неизменность старых маршрутов; все title/summary/images/href в4вариантах; настоящий локальный state ширины и переключения; Stories для этих сценариев, keyboard/focus/узких ширин; typecheck, обе сборки, относительные ссылки, diff/whitespace и baseline. Статическое ревью контраста, crop, типографики, полноты и границ страницы. Фактический mobile/browser QA не подменяется рендером React.

## Result

**Итог:** добавлены четыре представления всех трёх Section и общий режим сравнения. Адрес — `http://127.0.0.1:5173/prozhito/#/lab/editorial-catalog`; прямой адрес сравнения — `#/lab/editorial-catalog/compare`. Переключатели вариантов — нативные ссылки, ширины — временный React-state; карточка целиком открывает настоящую историю. Desktop-компенсация глобального отступа ограничена production `#root`, поэтому не расширяет Storybook. Заголовок вкладки лаборатории установлен локально. Ни один дизайн не принят, продуктовый контракт не изменён.

**Файлы:** 7 новых файлов в `src/apps/editorial-catalog-lab`, новый packet, точечная регистрация маршрута в `src/router.ts`/`src/App.tsx` и описание отдельного эксперимента в `docs/EDITORIAL_V2.md`, `harness/PROJECT_STATE.md`, `harness/WORKBOARD.md`. Итого 13 файлов в write set. Новых зависимостей, production assets и сетевых операций нет. Коммит, push и публикация не выполнялись.

**Проверки:**

- `npm run build` (включает `tsc --noEmit`) — PASS. Лаборатория собрана отдельным lazy-chunk. Прежнее предупреждение основного bundle >500kB осталось; unrelated оптимизация не выполнялась.
- `npm run build-storybook -- --output-dir /tmp/prozhito-catalog-lab-storybook` — PASS; в индексе все 6 Stories: Original, Split, Background, Overlap, Compare, Narrow320. Предупреждение sandbox о невозможности записать глобальный `~/.storybook/settings.json` не помешало сборке.
- `/tmp/prozhito-catalog-lab-check.mjs` — PASS: 22 чистых React-render (12 отдельных карточек, 5 страниц и 5 входов через route-компонент); проверены полные title/summary, существующие изображения, одна нативная ссылка на карточку, 3/12 карточек, текущий вариант и исходная ширина390. Это разметка без HTTP/listen/browser.
- Parser — PASS: default split, 5 прямых адресов, регистр и trailing slash, отклонение лишних сегментов; прежние каталоги/versions/лаборатории, все44 архивных адреса и6 редакционных адресов сохранены. Каноническая коллекция:22 Section,6 уровней, распределение3/4/4/4/4/3, по9 звеньев.
- Stories содержат проверки реального переключения ширины Space/Enter/Tab, `aria-pressed`, полного текста и href. Они скомпилированы, **не выполнялись в браузере**. Narrow320 обозначает контейнерную пробу, не эмуляцию viewport.
- Независимое read-only ревью — PASS после исправления document.title, root-only компенсации margin и различимого focus-offset в forced colors. В B весь текст заканчивается до начала прозрачной зоны; высота текстов не фиксируется; навигация и состояния не требуют указателя.
- `git diff --check`, `git diff --cached --check`, отдельная проверка whitespace новых файлов и относительных Markdown-ссылок — PASS.
- SHA256-baseline `/tmp/prozhito-catalog-lab-before.json`: из существовавших файлов изменены только пять объявленных интеграционных/документационных файлов; остальные307 совпали. Production-компоненты editorial_v2, архив, канонический контент, иллюстрации и прежние лаборатории не изменились. Локальный процесс разработки слушает127.0.0.1:5173 (проверка процесса, не HTTP).

**Сверка композиционного ориентира с реализацией:**

| Деталь | Реализация и свидетельство | Статус визуальной проверки |
| --- | --- | --- |
| A: текст слева / портрет справа | CSS-grid, изображение92–120px, gutter20px, высоту задаёт полный текст | Браузерный screenshot недоступен |
| B: изображение растворяется в бумаге | Справа44% кадра; непрозрачная подложка заканчивается правее всей текстовой колонки | Crop и фактический контраст на устройстве требуют просмотра |
| C: наплыв бумажного поля | Кадр160–180px, текстовое поле заходит на32px снизу | Реальный жест/черновик в crop требуют просмотра |
| Типографика и плотность | Нынешние serif/sans; compact title26–28px, summary14px; без обрезки строк | Переносы на320/390/430px ещё не подтверждены |
| Содержимое | Все3 настоящих title/summary/assets/href подтверждены React-render | Нового растра, вымышленного контента и сокращений нет |
| Цвет и границы | Токены EditorialTheme, бумага, лёгкие разделители, без теней/скруглений | Визуального сравнения в браузере нет |
| Отличия от генеративного черновика | Намеренно убран чрезмерный зазор перед CTA; используются исходные иллюстрации, не перерисованные детали эскиза | Эскиз просмотрен через view_image, не является утверждённым финалом |

**Оставшиеся риски:** реальный mobile/browser QA, переносы при увеличении текста, crop и видимость клавиатурного фокуса требуют проверки после снятия URL-policy ограничения. Не выполнялись обходы через другой браузер, порт или HTTP. Процент экономии высоты, pixel-perfect соответствие и визуальная оценка10/10 не заявляются. Это готовая к просмотру лаборатория с ограниченной проверкой, не release-pass или принятое изменение каталога.

**Следующий шаг:** просмотр лаборатории владельцем; перенос выбранного дизайна в каталог — только после отдельного решения.

## Уточнение C: номер на изображении

Запрос владельца: развить вариант «Наплыв», убрать отдельную строку номера и вынести его в верхний левый угол картинки. Write set этой итерации: `CatalogCard.tsx`, `CatalogCard.module.css`, README лаборатории и этот packet. Остальные варианты, рабочий каталог, контент, кадрирование и маршруты вне scope.

Номер перенесён в изображение только у C: небольшой прямоугольный бумажный фон без рамки и скругления, отступ20px совпадает с отступом текста. Это декоративная нумерация внутри прежней единственной ссылки, не отдельная кнопка. Текстовое поле начинается сразу с заголовка. Результат — done в пределах лабораторной пробы; перенос в рабочий каталог не утверждён.

Проверки уточнения: `npm run build` и существующие22 React-render — PASS; независимое статическое ревью подтвердило единственность номера, непрозрачную подложку и отсутствие пересечения с фокусом. Baseline `/tmp/prozhito-catalog-overlap-number-before.json` подтвердил ровно4 объявленных изменённых файла. Whitespace, `git diff --check` и `git diff --cached --check` — PASS. Браузерный просмотр/переносы/визуальный crop не проверены из-за прежнего URL-policy ограничения; новых UI-тестов для этой точечной правки не добавлено.

## Два положения номера: C / D

Следующий запрос владельца: у C прижать номер вплотную к верхнему левому углу картинки; добавить ещё один наплыв D, где номер на50% своей высоты поднят над верхней границей наплыва. Обе подложки сохраняются. Интерпретация D объявлена перед правкой: номер находится на верхнем крае текстового поля, половина подложки выше него. Различается только расположение номера, без новых изображений и сокращений текста.

Write set: `model.ts`, `CatalogCard.tsx`, `CatalogCard.module.css`, `CatalogLabApp.tsx`, `CatalogLab.stories.tsx`, README лаборатории, `docs/EDITORIAL_V2.md`, `harness/PROJECT_STATE.md`, `harness/WORKBOARD.md`, этот packet. Stories — отдельный автор build_editorial_exercise, остальное — root; review_integration_contract работает read-only. Основной каталог, Section, архив, прочие лаборатории, варианты0/A/B, иллюстрации и внешняя публикация вне scope.

План проверки: существующие проверки рендера/маршрутов обновить для пятого варианта, собрать приложение и Storybook, проверить каскад фокуса и неизменность файлов вне scope. Browser/mobile QA остаётся заблокированным прежним URL-policy; обходов нет. Статус итерации — done в пределах лабораторной пробы.

Результат: C (`overlap`) теперь имеет номер top0/left0 относительно картинки. Добавлен D (`overlap-raised`), где номер располагается внутри текстового поля: top0, left по отступу текста, translateY(-50%). У обоих светлая непрозрачная подложка. Общие высота/crop изображения, наплыв32px, текст и геометрия не меняются. Номер по-прежнему один на карточку; `CatalogCard.tsx` не потребовал правки — его существующая условная разметка уже помещает D в текстовый блок. Поверх подложек сохраняется видимое кольцо клавиатурного фокуса. Всего5 вариантов, в сравнении15 карточек; новый вариант имеет собственную ссылку и Story. Изменений продуктового контракта нет.

Проверки итерации:

- `npm run build` / typecheck — PASS, прежнее предупреждение размера основного bundle осталось.
- Storybook build — PASS,7 Stories включая OverlapRaised; предупреждение sandbox при записи глобальных настроек не препятствовало сборке. Plays скомпилированы, не запускались в браузере.
- Существующая проверка `/tmp/prozhito-catalog-lab-check.mjs`, адаптированная к количеству вариантов, — PASS:27 React-render, реальные тексты/ссылки/изображения, default/прямые маршруты и прежние маршруты сохранены; канонические22 Section с9 звеньями не изменены.
- Независимое статическое ревью C/D — PASS: по одному номеру/ссылке, подложки и фокус, расположение D относительно copy без обрезки;0/A/B не затронуты.
- Baseline `/tmp/prozhito-catalog-overlap-raised-before.json`, scoped проверка Stories, относительные ссылки, whitespace и оба `git diff --check` — PASS. Правки только в объявленном наборе; production, архив и исходные assets не менялись.
- Реальный browser/mobile QA, переносы и визуальные слои остаются непроверенными из-за прежнего ограничения сессии. Перенос C/D в рабочий каталог не утверждён; публикации/коммита/push нет.
