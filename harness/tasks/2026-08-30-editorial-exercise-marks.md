# Task / Result Packet

## Task

**Название:** Размер мобильных штрихов и спокойные состояния desktop

**Владелец:** Стефания (`/root`); `audit_header_navigation` — read-only review.

**Статус:** done

**Цель:** Высота разделителей мобильной ленты2.25rem; на desktop нет рамки текущего пункта меню и левой метки выбранного ответа.

**Источники:** просьба и референс владельца от 30 августа2026; [AGENTS.md](../../AGENTS.md), [PROJECT_STATE.md](../PROJECT_STATE.md), [UI-контракт](../../docs/EDITORIAL_V2.md#упражнение-l01-s01), [меню до краёв колонки](2026-08-30-editorial-exercise-desktop-rail.md).

**Write set:**

- `src/apps/editorial-v2/components/EditorialExercise/EditorialExercise.module.css`;
- `docs/EDITORIAL_V2.md`;
- этот packet.

**Вне scope:** логика и JSX, контент, шапки, архив и остальные Section; глобальный focus, новые зависимости/данные/хранение, commit/push/publication.

**Уточнение исполнения:** сохраняются тонкие серые разделители строк и внешний border workspace, видимые в референсе. Удаляется акцентная рамка именно текущего пункта меню. Левый штрих выбранного ответа удаляется на desktop по явной просьбе, даже хотя он ещё виден на приложенном референсе; mobile сохраняет эту метку и нижнюю линию текущего пункта. Focus-visible и forced-colors outlines остаются.

**Риски:** задеть mobile при изменении base-CSS, убрать видимый keyboard focus, оставить устаревшее описание рамки/метки в контракте.

**План проверки:** desktop1280px и769px: отсутствие рамки/метки, сохранность заливок и управления; mobile390px и768px: разделители2.25rem, метка ответа и нижняя линия сохранены; build, review, ссылки/diff/baseline. Новые тесты для CSS-правки не добавляются.

## Result

**Итог:** мобильный штрих имеет высоту2.25rem. У desktop-кнопок ленты `border:0`; акцентная рамка текущего пункта удалена. Псевдоэлемент выбранного ответа перенесён в mobile media query, поэтому на desktop левой метки нет. Заливки, серые разделители, размеры/поля, native radio, focus-visible и forced-colors сохранены. Обновлены три файла write set; UI-контракт больше не обещает рамку или боковую метку на desktop.

**Проверки:**

- Browser1280×900: текущая кнопка без border, фон текущего пункта и выбранного ответа сохранён; псевдоэлемент ответа `content:none`. Меню и кнопки240px, тонкие разделители1px, переполнения нет. Скриншот `/tmp/prozhito-exercise-marks-desktop.jpg` визуально проверен.
- На границе769px — desktop, border0 и метки нет; при768px и390px — mobile, штрих36px, нижняя линия текущего пункта2px и метка ответа3px сохранены. Переполнения нет. Скриншот `/tmp/prozhito-exercise-marks-mobile.jpg` визуально проверен.
- Переход вперёд/назад сохраняет выбранный ответ. Общий контейнер остаётся без outline. Browser console error/warn —0.
- Production build с TypeScript — PASS, остаётся прежнее предупреждение о chunk больше500kB. Read-only review CSS-каскада — без существенных замечаний.
- `git diff --check`, `git diff --cached --check`, whitespace трёх файлов и16 относительных ссылок — PASS. Из baseline234 файлов изменён только CSS упражнения; контент, JSX/Stories, архив и остальные Section неизменны.

**Ограничения:** локальный UI-check, не полный release-pass. Focus-visible/forced-colors проверены по CSS/review без повторной эмуляции; полный набор Stories и физические устройства не перепроверялись для этой CSS-правки. В обычном desktop-режиме выбранный ответ визуально выделяется только фоном, как запрошено; семантика checked и контур в forced-colors остаются.

**Следующий шаг:** оценка владельцем текущего второго экрана; автоматического переноса, commit/push/publication нет.
